{
  "name": "codex-remote-worker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "check": "node scripts/check-embedded-page.cjs && tsc --noEmit && wrangler deploy --dry-run",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240909.0",
    "typescript": "^5.6.2",
    "wrangler": "^3.78.0"
  }
}
